name: build mp3play

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: setup-psptoolchain
        uses: ./

      - name: build mp3play
        run: make
